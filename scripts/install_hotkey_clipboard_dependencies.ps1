# Correctr Hotkey Clipboard_v0.1 dependency install script
# Run this from the Correctr project root after activating your virtual environment.

python -m pip install --upgrade pip
python -m pip install pynput pyperclip pyautogui pytest
